{% extends "login_auth/layouts/header.djt" %}
{% load staticfiles %}
{% block content %}

<div class="ui container">
	{% if message %}
	<div class="ui message">
		<i class="close icon"></i>
		<div class="header">
		  Network Error
		</div>
		<div>
		  <p>{{message}}</p>
		</div>
	</div>
	{% endif %}

	<div class="ui fluid segment">
		<form class="ui form" action="/signin/" method="post">
			{% csrf_token %}
			
	        <div class="field">
	              <label>UserName</label>
	              <input name="username" placeholder="Username" type="text" id="username">
	        </div>
	        <div class="field">
	              <label>Password</label>
	              <input name="password" placeholder="Password" type="password" id="password">
	        </div>
	        <div>
	        <center><button class="ui button green" type="submit" id="signin">Sign In</button></center>
	        </div>
		</form>
	</div>
	<br><br>
	<div class="ui fluid segment">
	<center>
	<p>Don't have an account ??? ...</p>
	<a href="/Register"><button class="ui button black">Sign up</button></a>
	</center>
	</div>

</div>

{% endblock %}